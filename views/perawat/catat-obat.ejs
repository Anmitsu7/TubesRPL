<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Catat Obat - Klinik SehatCare</title>
    <link rel="stylesheet" href="public/css/halaman-catat-obat.css">
</head>
<body>
    <header>
        <h1>Klinik SehatCare</h1>
        <nav>
            <a href="/halaman-perawat">Halaman Perawat</a>
            <a href="/diagnosa">Hasil Diagnosa</a>
            <a href="/logout">Log Out</a>
        </nav>
    </header>

    <section>
        <h2>Catat Obat untuk Pasien Hari Ini</h2>
        <% if (daftarPasien.length === 0) { %>
            <p>Belum ada pasien yang dijadwalkan untuk menerima obat hari ini.</p>
        <% } else { %>
            <form action="/proses-catat-obat" method="POST">
                <table>
                    <tr>
                        <th>Nama Pasien</th>
                        <th>Obat yang Diberikan</th>
                        <th>Aksi</th>
                    </tr>
                    <% daftarPasien.forEach(function(pasien) { %>
                        <tr>
                            <td><%= pasien.pasien %></td>
                            <td><input type="text" name="obat_<%= pasien.pasienId %>" placeholder="Nama Obat" required></td>
                            <td><button type="submit" name="submit" value="<%= pasien.pasienId %>">Catat Obat</button></td>
                        </tr>
                    <% }); %>
                </table>
            </form>
        <% } %>
    </section>

    <footer>
        <p>&copy; 2024 Klinik SehatCare. All rights reserved.</p>
    </footer>
</body>
</html>
